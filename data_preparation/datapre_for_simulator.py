# receive img directly from the simulator, before it is feed in our end to end model,
# we need to perform some preprocessing on it. so it can match the required size of resnet50,
# and keep the same process as training stage as well, so it'll be promised that our model can run successfully.


